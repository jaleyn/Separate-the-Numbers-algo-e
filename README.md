# Separate-the-Numbers-algo-e
The function tries all possible starting numbers and checks if the rest of the string forms consecutive increasing numbers. If a valid sequence is found, it prints "YES" with the first number; otherwise, it prints "NO".
